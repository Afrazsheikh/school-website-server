var user = require('./user');
var school = require('./school');
var admin = require('./admin');
var album = require('./gallery');
var form = require('./forms');
var feedbackForm = require('./feedbackform');

module.exports = {
    admin,
    user,
    school,
    album,
    form,
    feedbackForm
}